# V4.5 coordinated primal-dual trial

The direct user request starts a new V4.5 phase after V4.02. All outputs stay in this auction branch. Earlier mechanisms, proofs, and certificates are frozen; only the outer navigation and inventory will be updated, with their prior bytes preserved.

The formal target remains the unrestricted measurable, integrable, randomized auction on `[0,1]^4`, with pointwise DSIC, ex-post IR, and item feasibility, including every exceptional report and tie. This phase uses the supplied formal statement as the mathematical contract; its older research-control directions do not override the user's current coordinated search request.

The starting incumbent is the actual V4.02 split-cost mechanism at `s=142/125`, with exact revenue in `[0.875249850964183643304405, 0.875249850964183643304409]`. The starting outer manifest verifies 912 stable files. Eight V4.02 mathematical replays and its predecessor preservation check passed again in this phase; the execution output is `baseline_replay.txt`.

Three concurrent routes are coupled through concrete lemmas and witnesses:

1. Replace a scalar exchange shift by localized piecewise-affine inverse threshold pairs, proving a complete menu extension before evaluating its global revenue effect.
2. Solve or improve conditional screening at actual residual capacities beyond `Q`, distinguishing an inner price defect from a necessary change in the other bidder's allocation.
3. Test common capacity supports for both bidders. A failure must be an exact violated inequality or complementary condition, and must be separated from failure of a chosen representation.

The new ingredients are functional boundary compatibility, exact conditional revenue defects after the split-cost change, and the behavior of singular capacity prices under opponent-null menu changes. Merely increasing grid size, a polynomial basis, or optimizer runtime is not a route.

Successful screening supports eliminate all randomized improvements only on the residuals they actually certify. A localized stationary menu is not an unrestricted optimum. A null-opponent-face change can invalidate a proposed common support while preserving auction revenue; that fact alone is not a profitable primal deformation.

The final package will distinguish exact feasible improvements, unrestricted conditional certificates, rigorous obstructions to particular common supports, numerical discovery, and the unresolved matching global upper proof. No broad literature sweep is reopened.
