# V4 verification record

Executed 5 September 2026, using CPython 3.10.16 without optimization (`-B -X utf8`).

## Mathematical replay boundary

`V4/verifier/run_all.py` passed. It ran seven mathematical V3 checks plus V3 preservation, five mathematical V4 checks, and V4 preservation: **12 mathematical replays and two preservation checks** in total. These include both the primary V3 algebraic/logarithmic enclosure and the independent polynomial/binomial enclosure. V4's exact checks cover the functional examples, complete bottleneck menu and support coefficients, free-fiber revenue, release mechanism and transfer box, and combined exact endpoint.

The final output was:

```text
V4_ALL_REPLAYS_PASS: 12 mathematical replays and 2 preservation checks including V3 dependencies
PRE_V4_PRESERVATION_PASS 778 identities; one mapped navigation file
```

The individual V4 mathematical scripts reconstruct and compare their saved certificates without writing by default. `--write` is explicit regeneration. The functional and screening authors each independently checked the other's revised read-only replay; saved certificate bytes were unchanged. The release reviewer separately checked all 16 corners of its positive-volume transfer box using exact arithmetic, in addition to verifying the analytic inequalities on the entire box.

The rational menu checks certify affine dominance on full one-dimensional intervals. General convexity, integration by parts, measurable-selection scope, infinite-tail arguments and the full-domain gap identity are established by written proofs and separately audited. Finite smoke checks and PASS messages are not formal machine proofs of continuum statements. No grid LP, optimizer, regularization, numerical tolerance, or finite-menu optimality assumption enters the new certificates.

## Independent proof audits

- `release_audit.md`: all-report utility truncation, strict-region splice, global `-2*epsilon` revenue bound, exact witness menus, actual item transfer, code-to-proof correspondence and positive-volume box.
- `functional_independent_audit.md`: global Borel elimination, admissible homogeneity, selected-subgradient constraints, singular support and no-countably-additive-support example.
- `screening_independent_audit.md`: full two-item competitor support, singular report values, minimizing-selector hypothesis, lottery, ties and exact reoptimized directional formula.
- `report_independent_audit.md`: synthesis scope, equality versus bound, all 18 inherited appendix constants/coefficient entries, radical endpoint identities and displayed exact lower/gap intervals.

The last audit's TeX hash matches the delivered source. These are independent bounded audits within this research session, not external peer review.

## PDF QA

The installed pdfTeX / TeX Live 2026 compiled the 10-page report. Optional unavailable `microtype` and `enumitem` packages were removed; none were installed. A type-domain macro and long equation layout were corrected before final compilation. The final log has no overfull/underfull boxes, undefined references or LaTeX errors.

Poppler rendered all ten pages at 105 dpi. Every page was visually inspected; no clipping, overlapping text, unresolved references or missing glyphs were observed. Bundled `pypdf` independently checked the ten-page count, nonempty extracted text, absence of replacement characters and the key endpoint strings. `certificate/pdf_qa.json` records the exact PDF/source hashes and page text lengths. Rendering and text extraction are presentation QA, not revenue or mechanism proofs.

Build/render scratch is inside the requested auction folder under the outer manifest's excluded `LaTeX/build/V4` and `LaTeX/qa/V4` paths. Final TeX and PDF are inside V4.

## Preservation and remaining scope

All 778 pre-V4 stable identities passed, with the previous `CURRENT_PHASE.md` bytes mapped to `V4/certificate/pre_V4_CURRENT_PHASE.md`. The rest, including all V3 mathematical files, remain at their original paths and unchanged. V4 and outer manifests are generated only after the final stable artifacts, using the preserved safe inventory function, then checked for hash identity and complete coverage.

The inherited unrestricted upper is carried forward with its preserved certificate; it was not newly derived or independently fully recertified in V4. No new candidate-specific supporting measure closes that upper gap. Only the free fibers of the improved candidates are fully optimized; the diagnostic type-dependent inner problem is fully solved at its own stated residual.
