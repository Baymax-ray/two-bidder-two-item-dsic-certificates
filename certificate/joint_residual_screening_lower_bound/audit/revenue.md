# Independent V4.6 revenue and portability audit

Verdict: the V4.6 exact revenue ledger and all newly added algebraic/logarithmic revenue terms passed independent reconstruction. The exact inequality is **0 < U - R < 1/100**. No revenue defect requiring a source change was found. This verdict concerns the specified mechanism's revenue, not unrestricted optimality; the all-real mechanism proof and inherited upper proof are separate audit workstreams.

All new files for this workstream are inside `V4_6_archive_audit/revenue`. The V4.6 source files and historical mathematical packages were read only. The V4.6 manifest passed again after the fresh arithmetic audit.

## Exact result

The inherited upper endpoint is

\[
U=\frac{3715139591287203}{4194304000000000}.
\]

The fresh rational radical/log evaluation gives the outward enclosure

\[
0.87581985414842245534607651887213333
\le R\le
0.87581985414842245534608051887213334.
\]

The corresponding gap enclosure is

\[
0.00993844837023212953856303581536666
\le U-R\le
0.00993844837023212953856703581536667.
\]

These strings are generated by integer floor/ceiling of rational bounds. The proof does not subtract floating-point displays. A shorter exact certificate uses

\[
L=\frac{8758198541484224553460}{10^{22}}\le R,
\qquad
\frac1{100}-(U-L)
=\frac{63028868882299352429}{1024000000000000000000000}>0.
\]

The code separately checks `R_upper < U`, so the gap is strictly positive as well. `fresh_exact_gap.json` contains the exact coefficient matches, source hashes, polynomial-residue coefficients, cell integrals, and positive integer numerator for this threshold certificate.

## Fresh reconstruction versus replay

`fresh_exact_gap.py` imports only Python standard-library modules. It does not import any source calculator. It reads the saved V4.5 historical revenue interval as a named dependency and reconstructs **all V4.6 additions** before comparing them with the saved source certificates.

1. The strip increment is integrated as an exact polynomial over the two source intervals.
2. The free-capacity and bundle contributions use direct area-times-payment menu formulas and explicit binomial evaluation at quadratic-field endpoints.
3. The final coupled Q-region contribution is integrated directly from the final menu to the original menu. This bypasses the source's separate bundle-exchange and reoptimized-extra additions. The resulting three coefficients exactly equal
   `[-1609541379668231661249547038251/159173905464000000000000000000000, 57567793/7593750000, -12167/1328906250]`
   in the basis `(1, sqrt(2), sqrt(23))`.
4. The joint lottery-cut integrand is integrated by recovering local Taylor coefficients at its rational poles. The source instead solves a rational linear system for partial fractions. The fresh method multiplies the result by the entire denominator and checks the exact polynomial identity, including all simple and double poles.
5. The upper entry-fee strip uses direct complete before/after three-price menu revenue polynomials. Its three exact cell integrals match the source individually, not only in total.
6. The lower entry-fee strip reconstructs the no-sale polygon from its three vertical slices, with breakpoints `c`, `c+L`, and the literal low singleton price `1/2+delta`. It checks the polynomial identity `3*D0-1 = alpha*(c+L/4)` before integrating the full fee variation.
7. The three logarithms use a fresh 80-term one-sided positive series after reciprocal reduction. Signed coefficients propagate the endpoints outward. Radical bounds use exact integer square roots at 70 decimal digits.

The exact joint rational part and all three log coefficients match the source. The first-bidder fee change is

\[
-24631898853429/125000000000000000000000.
\]

The complete joint change is enclosed by

`0.000000003916127942142253666897026142330621130...`.

It strictly exceeds the separately retained conservative rational gain bound `26902077489/12500000000000000000`.

The first fresh-script run failed because this audit's draft no-sale polygon mistakenly used low singleton price `1/2`. Inspection of the literal source menu showed `1/2+delta`; correcting the audit code restored the exact identity. No source quantity or source proof was changed to obtain a pass.

## Hostile tariff and logarithm checks

The literal V3 tariff table is a necessary data dependency. The source primary joint verifier scans zero-low-rho rows and verifies its normalized coordinate is below `1/8`. The fresh audit also checks the omitted complementary condition: **every other tariff row intersecting the complete fee-strip sum range has a lower rho endpoint at least `1/8`**. All 41 table rows are covered by this exclusion test. Thus a nonzero-low-rho row cannot silently enter the integration domain.

Only `B13.1` and `B14.1` have zero rho lower endpoints in this range; their literal sum intervals and fees match the three affine integration cells. The subsequent zero-fee region is therefore justified by the table, rather than extrapolated from a sampled tariff. The line `y=c` has a separate retained menu in the mechanism; it is a null set for revenue and must remain included in the pointwise mechanism proof.

The rational denominators have no pole on `[7/10,71/100]`. Endpoint ratios are positive and exactly `161/176`, `613/628`, and `13/10`. The fresh calculation handles the signs of the two ratios below one through reciprocal reduction. The source's signed-tail and outward decimal bounds contain the tighter fresh values.

## Executed checks

Execution used Python 3.10.16, assertions enabled, `-B -X utf8`, no optimized execution, and no discovery solvers.

| Check | Result | Evidence |
|---|---|---|
| Original V4.6 combined read-only runner | PASS: 11 new and 14 inherited mathematical replays; 945 predecessor identities | `run_all.log` |
| Original V4.6 manifest | PASS: all 47 stable files | `manifest.log`; repeated after fresh audit |
| Fresh arithmetic calculation and audit JSON creation | PASS | `fresh_exact_gap.log` |
| Fresh arithmetic default read-only replay | PASS, exit 0 | tool execution record; same output as `fresh_exact_gap.log` |
| Dependency tracing of 25 current mathematical entries plus four historical revenue entries | PASS, 29 entries | `dependency_trace.log`, `dependency_traces/*.log` |
| Exact historical revenue extras | V3 baseline, V3 joined primary, V3 joined independent, V3.1 independent all passed | individual dependency trace logs |
| Dormant helper-module standalone paths | PASS, both exit 0 | `static_free_capacity_replay.log`, `static_constrained_candidate_replay.log` |

The historical extras exercise the earlier revenue chain rather than merely trusting the V4.5 displayed number. They remain replays of historical arithmetic and certificates, not a newly written proof of every historical continuum partition.

## Portable dependency closure

`dependency_closure.json` is the machine-readable runtime closure: **61 files, 334391 bytes**, all within the auction package. It includes each entrypoint, each actually read/imported source file and certificate, its byte count, and its SHA-256. No network resource, outside-workspace file, discovery solver, or source-writer path was required. Per-entry records make the dependency relation auditable.

The traced closure is complete for the explicitly listed 29 replay entrypoints. It is not a claim that every dormant function in every shipped module can be invoked with those 61 files alone. Static path/read inspection identifies two additional small JSON dependencies if standalone execution of every imported helper module is desired:

- `V4/certificate/combined_revenue.json`, read by the dormant main path of `V3_1/verifier/constrained_candidate.py`;
- `V4/certificate/free_capacity_fibers.json`, read by the dormant main path of `V4/verifier/free_capacity_fibers.py`.

These two conservative additions are listed and hashed in `dependency_static_supplement.json`. Retaining them yields a 63-file source/certificate closure of 337269 bytes. Both standalone main paths were also freshly replayed and passed. `dependency_static_references.txt` lists source import, `Path.parents`, file-read and subprocess references with line numbers.

Preserve the existing sibling version directory layout under one new archive-local parent. The final mechanism chain is

`V4_6/price_joint_reallocation -> outer_bundle_reoptimized -> outer_bundle_exchange -> free_bidder_one -> outer_lottery_strip -> V4_5/residual_lottery -> V4_02/split_cost_candidate -> V3_1/constrained_candidate -> V4/free_capacity_fibers -> V3/joined_threshold -> baseline_mechanism`,

with the V3 literal tariff JSON and the independent revenue modules recorded in the manifest. Relative version layout, not the original absolute workspace path, determines the read locations. Run each replay in a fresh subprocess because reused module names and manipulated `sys.path` can otherwise cross version boundaries.

The original versioned `run_all.py` and `preserve_previous.py` scripts are intentionally not part of this minimal runtime closure. Their preservation paths scan the old 912/945-file source inventories. Shipping them as the archive's executable entrypoint would require vendoring that whole historical inventory. The archive should instead have its own runner over the named mathematical entries and its own manifest, while retaining the already completed source preservation logs and source-to-archive byte mapping as provenance.

Written all-real proofs must also be retained. For V4.6 the necessary proof spine is the strip, reverse-free, bundle-exchange, coupled-response and joint-reallocation notes, together with the exact-revenue and fee-audit notes. The two conditional inner theorems and conditional coverage note should accompany any claims made about inner optimality. Runtime read tracing alone cannot establish a documentation/proof closure; the primal/dual audit workstreams own that review. The inherited unrestricted upper certificate is also separate from these 61/63 lower-mechanism files.

## Remaining boundary

The mathematical conclusion supported by this workstream is a specified mechanism revenue with an exact strict gap below `0.01`, conditional on the separately checked feasibility/DSIC/IR proof and inherited upper certificate. It does not supply a matching unrestricted dual, determine unrestricted optimal revenue or attainment, or promote conditional inner optimality to full-auction optimality. No archive copy or source edit was performed in this workstream.
