# Final report and README audit

The report and README agree with the frozen selected mechanism, its
independent structural proof, and the exact result ledger. No
mathematical blocker was found. This audit makes no changes to candidate
code, numerical certificates, predecessors, or manifests.

## What was checked

- Read the entire `REPORT.md` and `README.md`, the selected
  `refined_candidate.py`, the full refined structural audit, the independent
  revenue certificate and final derivation, the conditional gap map, and
  the new W screening proof. Read the combined replay's command list to
  check the README's execution scope.
- Recomputed the candidate source SHA-256 and matched it to
  `certificate/branch_summary.json`.
- Matched the report's three exact revenue coefficients and displayed
  revenue/gain enclosures to the ledger and independent revenue result.
  The radical conversion is exact: sqrt(246947/1125000)=sqrt(493894)/1500.
  The displayed strict gain exceeds 17/2000000 by exact rational
  comparison. This is a consistency audit of the independently derived
  revenue, not a third independent continuous integration.
- Independently recomputed the Q, E and W planar areas from the displayed
  rational parameters, their union and complement. Every fraction matches
  the report and ledger. The report correctly identifies these as
  opponent-report areas, not portions of the global revenue gap closed.
- Checked the closed Q / open E conditions, exact jump at c, all inverse
  branches, the capped Q safe price, retained-base fee placement,
  orientation priority, empty-at-zero rule and complete lexicographic
  feasible-pair selection against the source.
- Checked the full-Q claim against actual occupied top and anchor traces
  and the cap case. The report correctly allows C0(k)>1; the envelope does
  not use a nonexistent C<1 premise.
- Checked that the E statement uses the incentive-derived trace from the
  occupied open rectangle, and does not falsely claim literal capacity
  occupation on the entire junction. Its actual-inner scope is distinct
  from a supporting measure for arbitrary residual perturbations.
- Checked the reported W region and sign margin against its separate
  full randomized screening proof. Its certificate eliminates an inner
  gap and adds no auction revenue increment. Its use of actual occupied
  regions is not presented as a common global auction dual.
- Checked the report's ownership-change witness and positive-volume
  explanation against the selected implementation's uniform scalar
  inequalities. The report distinguishes those inequalities from the
  supplemental 16 corner checks.
- Checked that alternative experiments and their cap/functional gains are
  not stacked onto the selected result, floating searches are discovery,
  finite profile checks do not prove continuum optimality, and the final
  unrestricted auction objective remains explicitly open.

## Documentation corrections

Two report-only clarifications were made:

1. Replaced the broad phrase that the reserve and split constants were
   re-optimized with the precise statement that a and c changed while
   d remained 1/2.
2. Specified that the positive fee on the rho=c face applies to retained
   base rows. Q rows use their stated Q menus.

The README needed no mathematical correction. Its read-only commands
exclude discovery and certificate-writing flags, use `-B`, and explicitly
prohibit optimized assertion execution with `-O`. The surrounding
predecessor directories are identified as trusted exact-arithmetic
runtime dependencies.

## Packaging boundary

The previously pending `VERIFICATION.md` reference is now closed. The file
exists, was read in full, and correctly describes the exact replay and its
independence boundaries. Every relative link in REPORT, README and
VERIFICATION now resolves to an existing local file.

The captured `research_log/final_replay.txt` records the selected mechanism,
independent structure and revenue checks, and the final completion marker:
14 new mathematical replays plus 12 preserved V4.6.1 replays passed. It also
records the unchanged 52-file V4.6.1 manifest/package, 992 older live
identities plus one matching archived navigation identity, and the 47-file
V4.6 manifest. VERIFICATION correctly reports current outer navigation
and manifest drift as outside this branch's managed scope. Its reported
64 positive-tie profiles matches the structural audit JSON.

VERIFICATION distinguishes all-real proofs from finite implementation
checks, alternative trial results from the selected revenue, actual-inner
certificates from arbitrary-residual supports, and hashes from mathematical
validity. The separate final artifact audit and branch manifest sealing
remain the root investigator's post-audit packaging steps; this note does
not pre-claim their execution.

The 52-file V4.6.1 preservation claim refers to its saved manifest; it is
not a claim that concurrent outer metadata or every historical live
navigation file was frozen. The report correctly confines managed output
to this version directory and keeps outer navigation and concurrent
branches outside its preservation scope.
