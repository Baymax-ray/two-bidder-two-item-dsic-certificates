# Active coordinated primal-dual certificate

The selected V4.6.1.1 mechanism and the V4.6.2 unrestricted upper produce gap <0.006459 and revenue >99.2684% of unrestricted optimal revenue. `manifest.json` specifies both exact endpoints and the current pairing. Historical source ledgers retain their original comparisons.

Run `python -B -X utf8 verify_coordinated.py`. Python >=3.10 and NumPy are required; optimized Python is rejected. The supported entrypoint verifies all source bindings, copies only sealed files into an isolated temporary mirror, executes 46 explicit mathematical entrypoints, checks that no copied proof input changed, and reconciles the endpoints against the fresh third revenue calculation. The two complete new upper traversals are included. No original research workspace, network, optimization solver, or discovery program is needed.

`source/` preserves source bytes, including historical research notes and alternative candidates. Only `entrypoints.json` defines the supported proof execution path. Raw source README reproduction commands may refer to workspace-preservation files omitted from this portable closure; use the wrapper above. Source preservation routines and discovery programs are not invoked. Inherited proof helpers are bound by `dependencies.json` and copied from the retained archive packages. The new endpoint does not depend on rerunning the older, slower upper integration, whose certificate remains retained separately.

Fresh audit additions independently reconstruct structural identities, the full algebraic revenue, support flux/divergence and a strict lottery-box slack for the latest mechanism. The source-level all-real proofs establish continuum quantifiers; finite profile checks alone do not. These are independent AI-assisted internal audits, not proof-assistant formalization or external peer review. Conditional optimality on 64.1627% of opponent space is not global optimality, and both charged values being zero does not provide complementary slackness for an auction.
